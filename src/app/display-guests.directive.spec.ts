import { DisplayFlimsDirective } from './display-guests.directive';

describe('DisplayFlimsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayFlimsDirective();
    expect(directive).toBeTruthy();
  });
});
